# 進捗


パケットの受信処理を行うスレッドを**受信スレッド**， パケットの解析処理を行うスレッドを**解析スレッド**と呼ぶことにする．
学士の卒業研究で作成した HTJ2K パケット用のパケット解析を行うプログラムを単に**パケット解析器**と呼ぶ．

## 各実行環境の一覧
|PC|OS|CPU|GPU|RAM|コンパイラ|備考|
|-|-|-|-|-|-|-|
|研究室|Ubuntu 24.04.4 LTS|13th Gen Intel(R) Core(TM) i9-13900H|-|32GB|GCC 13.3.0|-|
|Raspberry Pi 3 model B+|Raspberry Pi OS 64 bit|Quad core ARM Cortex-A53|-|4GB|GCC 14.2.0|-|
|Raspberry Pi 4 model B|Raspberry Pi OS 64 bit Debian GNU/Linux 13 (trixie)|ARM Cortex-A72|-|8GB|GCC 14.2.0|-|
|ノートPC1|Windows 11 WSL2 Ubuntu 24.04 LTS|11th Gen Intel(R) Core(TM) i7-1165G7 @2.80GHz|-|16GB|GCC 13.3.0|電源モード バランス|
|ノートPC2|Ubuntu|-|-|4GB|GCC|常時 AC アダプタ使用|
|デスクトップPC|Windows 11 WSL2 Ubuntu 24.04 LTS|AMD Ryzen 7 7800X3D 8-Core Processor 4.20 GHz|AMD Radeon RX 7800XT|32GB|GCC 13.3.0|-|

## 2026/04/07

### プログラム 
HTJ2K パケットを解析するプログラムを作成．  
RTP パケットを解析するプログラムを作成．  
上記で作成した関数を使用して， HTJ2K パケットと RTP パケットの両方を解析するプログラムを作成．
.rtp ファイルを指定のアドレスとポートに送る， kakadu の kdu_stream_send に似たツールを実験用に作成．  

### テスト

動作確認のために RTP パケットの間隔を十分に空けて，HTJ2K パケットの解析結果のログを取った．  
ログを動作確認済みの HTJ2K パケット解析器のログと比較．  
結果，解析処理は正しく動作した．  

性能評価として kakadu の kdu_stream_send と自作したツールの両方で 60fps 相当でパケットを送信したところ，
RTP パケットのシーケンス番号の欠落が起こった．  
したがって， 60fps の映像には解析速度が不十分であることがわかった．

### 考察

解析速度のボトルネックをいくつか考える．
- パケットを跨いで符号化データが送信されるが，
後続のハードウェアデコーダでの処理を見越して別のバッファにデータをコピーしている．
- HTJ2K パケットヘッダの解析用にバッファから n byte または n bit ごとに取り出す関数を使用しているが，パケットヘッダについても RTP パケットと跨ぐため，データのアクセス前にチェック処理が入る．
- 上記の関数では，範囲外にアクセスしようとしたときに， socket2.h, recv() 関数を実行した後で，参照するバッファを変更するため直前の処理時間の影響を受けやすい．
RTP は UDP 上でのプロトコルのため，直前の処理に時間がかかっているとパケットが欠落し，結果として RTP のシーケンス番号のズレにつながる．
- perf の結果からはメモリのコピー， get_bit() 関数， read_packet_header() 関数に時間がかかってるらしい．ただ perf の結果では一番パーセンテージが大きい関数でも 5% 前後．

改善点として， RTP パケットの受信処理は非同期処理にする．メモリのコピーも場合によってはスレッドを分ける．

## 2026/04/08

### テスト

上記でのメモリコピーの処理をコメントアウトし，同様の実験を行ったところ，エラーが出たパケットの位置に変化はなかった．
したがって，パケットの受信タイミングに原因があると考えられる．

### 考察

現状の RTP パケット受信のタイミングは HTJ2K パケットの解析処理に埋め込まれているため，受信のタイミング次第でパケットが欠落していると考える．  
RTP パケットの受信と HTJ2K パケットの解析を非同期を非同期処理に分離することで，この問題に対処する．

## 2026/04/13

### プログラム

他のディレクトリで非同期のバッファ処理のたたき台を作り， branch fuature-thread に持ってきた．
この処理は **leaky bucket model** と呼ばれる方式である．
バッファの書き込み待ちの処理に 
```C++
while (next_pop->empty()); // データの排出が入力より速いため，データが入力されるまで待機
```
と書いたため最適化で待機処理が消された．copilotくんいわく，上記の処理は最適化の際に消されるらしい(コンパイラは別スレッドのことを知らないため，値が変わらない or 無限ロープと見なす)．  
一部の変数を atomic 操作に変更したところ，ログ出力なし -O3でたまに動くようになった．

### 考察

[条件変数の利用方法](https://cpprefjp.github.io/article/lib/how_to_use_cv.html) によると，
上記のような実装は**ポーリング方式**と呼ばれ，参照側スレッド(今回ではパケット解析器本体側)が常に動作しリソースを消費するため，
実行効率が著しく下がるらしい．  
実際に perf を使ったところ，上記の while 文にかなりの cpu を使っている可能性があった．  
解決策として，mutex と condition_variable を用いた std::condition_variable::wait での実装に切り替える．

## 2026/04/14

### プログラム

他のディレクトリでステート方式のバッファを作り， branch fuature-thread に持ってきた．
[cpprefjp の説明](https://cpprefjp.github.io/article/lib/how_to_use_cv.html)にあやかって**ステート方式**と呼ぶことにする．
バッファ自体のテストは正しく動いている．
しかし，バッファの中身のデータ(受信したパケットのデータ)は同期処理を行っていない．
そのためバッファのデータの処理中にデータを他スレッドに書き換えらているような挙動を取った．
その根拠として，empty packet でプログラムが停止した時のデバッグ用変数 call_count が毎回異なる値を取っており，
なおかつ，RTP の extended_sequence_number を用いたアサーションではプログラムが停止しないためである．
また，current_num_data < NUM_BUFFER のアサーションに失敗している．

***追記1*** 追加の検証として，バッファからの排出処理の同期にタイムアウト(1ms)を設定したところ，
明らかに解析スレッド側が過剰にブロッキングされてことが確認できた．

***追記2*** さらに解析スレッドの処理をデータの取りだしのみに変更し， RTP の extended_sequence_number の値が飛んだときの値を観察した．
受信スレッドの while ループを短く(10us)スリープさせたところ， extended_sequence_number は 200~600 前後の範囲で飛んだ．
受信スレッドの while ループをスリープさせずに，同様の実験をしたところ， 10~90 前後の範囲で値が飛んだが，一度 2001 の値で飛んだ．


### 考察

今回の問題は，バッファのデータの寿命(ライフタイム)がパケット処理のスレッドの動作よりも短いことが原因だと考えられる．  
考えられる解決策として，

- ~~受信用のバッファを更に大きくすることで，データの寿命を延ばす．~~
- 一時的にスタックにデータを移すことで，寿命を延ばす．
- 根本的な解決策として，パケット処理側をさらに高速化させる．
- ***追記1*** 適切に同期処理を行い過剰なブロッキングが起こらないようにする．

などが考えられる．

１つ目の解決策では，解析スレッドの処理速度が受信スレッドよりも遅い場合，
データの排出が間に合わず，バッファの不足を遅らせるだけで，解決にはならない．

***追記1*** 原因として考えられることをいくつか上げると，

- 受信スレッドは最小限の while ループと受信用関数を用いており，
mutex::unlock の呼び出し後に再度 mutex::lock を呼び出すまでの間隔が短すぎる．
- ステート方式のバッファのテストが不完全・不十分．
- mutex の所有権を持った状態で socket2.h/recv 関数を呼び出している．

まず考えられる解決策として，受信スレッドの while ループに sleep 関数を組み込んで解析スレッド側のブロッキングの解除を行えるようにする．
ただし，UDP を受信する都合上，次の UDP パケットの到着までに再度 socket2.h/recv 関数で待機する必要があるため慎重に実装する必要がある．

また mutex を各要素ごとに管理する方もあるが， std::mutex のサイズを加味するとメモリ使用量が気になってくる．

mutex の所有権を持った状態で socket2.h/recv 関数を呼び出すということは，パケットを受信するまで，
mutex の所有権を手放さないということになる．
つまり，解析スレッド側で mutex::lock を呼び出している場合，パケットを受信するまで解析スレッドがブロックされてしまう．

***追記2*** このときの，受信用のバッファの最大数は2001で設定していた．
つまり， extended_sequence_number の値がバッファ一周分飛んだことになる．  
したがって，完全に解析スレッド側が過剰にブロッキングされている．
その原因は上記でも考察したように，受信スレッド側の mutex の所有時間が長過ぎることだと考えられる．

## 2026/04/15

### プログラム

2026/04/14 の考察で挙げた mutex のロック時間の問題を改善した．
今までは， mutex のロックを行った後に，解析スレッドと共有するバッファを用いて socket2.h/recv 関数を呼び出していた．
改善後は， socket2.h/recv 関数の呼び出しにテンポラリ用のバッファを用いて，
mutex のロック後に解析スレッドと共有するバッファに**コピー(std::memcpy)** を行うように変更した．  
この変更によって，**マシンパワーがある場合に限っては** 60fps の解析が可能となった．
しかし，1つの RTP パケット毎に最大 1384 byte のコピーが発生するのは無視できないボトルネックになる可能性がある．

perf を用いた計測(表1)では J2kBuf::get_bit(const uint8_t&) 関数に約 15% ， J2kBuf::get_bit() 関数に約 12% ほどの オーバヘッドが発生していることもわかった．
また カーネル処理の _copy_to_iter 関数が約 5% ほどのオーバーヘッドを示した．

#### 表1 perf report から一部抜粋 
|Overhead|Command|Shared Object|Symbol|
|---|---|---|---|
|14.95%|j2kpay|j2kpay[.]|J2kBuf::get_bit(unsigned char const&)|
|11.75%|j2kpay|j2kpay[.]|J2kBuf::get_bit()|
|5.48%|j2kpay|[kernel.kallsyms][k]| _copy_to_iter|
|5.42%|j2kpay|j2kpay[.]|PrecinctSubband::read_packet_header(J2kBuf*, unsigned char)|
|3.70%|j2kpay|j2kpay[.]|leaky_bucket_buf::pop(unsigned char*&)|

### 考察

perf report の結果から，まずは J2kBuf::get_bit 関数の処理を改善するはスループットの向上に必須であることがわかる．
解析スレッドの処理は基本的には，パケット解析器のソースコードを用いている．
しかし，J2kBuf::get_bit 関数(J2kBuf::get_byte関数も同様)では，
RTP パケットに分割された HTJ2K パケットを解析処理するために，終端時にバッファを更新する機能やそれに伴う終端チェックなどで処理時間が増加している可能性がある．
(バッファ更新が実際に J2kBuf::get_bit() 関数に含まれ計測されているかは，未検証)

#### 表2 perf report から j2kpay の抜粋

|Overhead|Comman|Symbol|
|---|---|---|
|14.95%|j2kpay[.]|J2kBuf::get_bit(unsignedcharconst&)
|11.75%|j2kpay[.]|J2kBuf::get_bit()
|5.42%|j2kpay[.]|PrecinctSubband::read_packet_header(J2kBuf*,unsignedchar)
|3.70%|j2kpay[.]|leaky_bucket_buf::pop(unsignedchar*&)
|1.87%|j2kpay[.]|Tile::read_packet(Precinctconst*,J2kBuf&)
|1.28%|j2kpay[.]|J2kBuf::make_packet_data(unsignedlongconst&,unsignedchar*)
|1.04%|j2kpay[.]|leaky_bucket_buf::receive()
|1.03%|j2kpay[.]|PrecinctSubband::init(Postion2D<unsignedint>const&,Postion2D<unsignedint>const&,Postion2D<unsignedint>const&,unsignedchar,unsignedchar,MultiMem::static_memory<2000000ul>*)
|0.89%|j2kpay[.]|Resolution::set_precinct(Postion2D<unsignedint>const&,Postion2D<unsignedint>const&,unsignedchar,MultiMem::static_memory<2000000ul>*)
|0.79%|j2kpay[.]|Tile::read(MainHeaderconst&,std::array<Precinct*,5670ul>&)
|0.76%|j2kpay[.]|CodeBlock::set_data(J2kBuf*)
|0.75%|j2kpay[.]|Resolution::empty()const
|0.70%|j2kpay[.]|Component::ceil_N_L(unsignedchar&,unsignedchar&)const[clone.part.0]
|0.60%|j2kpay[.]|Precinct::init(Subbandconst*,unsignedchar,Postion2D<unsignedint>const&,Postion2D<unsignedint>const&,Postion2D<unsignedint>const&,unsignedint,unsignedchar,unsignedchar,MultiMem::static_memory<2000000ul>*)
|0.55%|j2kpay[.]|Precinct::get_psubband_ptr(unsignedchar)const
|0.25%|j2kpay[.]|main::{lambda()#1}::operator()()const

## 2026/04/16

### プログラム

最適化を行うために，コンパイラオプションの見直しをした．  
get_bit 関数内での終端チェック時に，次にパースするバッファの値のコピーを保持するようにした．
これによって， 1 bit 毎に配列演算子によるポインタ演算の回数を少なくした．

RTP パケットを送信するための RTPsender を 1つの rtp ファイルから，記録されている以上のパケットを送信できるように改良した．
動作としては，rtp ファイル内のフレームの送信が完了した後，rtp ファイルの先頭フレームから送信をループさせる．
このとき， RTP パケットのシーケンス番号のみ，インクリメントし続ける．
この機能によって長時間で，スレッド間で共有しているバッファが不足するかどうかを検証できると考えられる．

### 検証

私物のノート pc を用いて，長時間の動作検証を行った．
環境は 

- Window 11 WSL2 Ubuntu 24.04 LTS
- CPU 11th Gen Intel(R) Core(TM) i7-1165G7 @2.80GHz
- RAM 16GB 
- バッテリ駆動，電源モードはバランス
- GCC 13.3.0
- ビルドタイプは RelWithDebInfo
- leaky_bucket_buf::NUM_BUFFER = 1000

RTPsender を用いて j2kpay の性能を測定．
2160p_5994fps_422_10bit.rtp を 60 fps, 24120 フレーム(=402s)送信した．

結果として，エラーは発生せず，バッファが不足するような現象は見られなかった．
また，ハードウェアデコーダへの書き込みを考慮して，コードブロック内の符号化データを別のバッファにコピーを行った．


### 考察

2026/04/15 時点では，perf による処理速度の低下に耐えられなかったが，今日に行った最適によって， PC 上での動作が安定するようになった．
コードブロックのデータのコピーについては， perf によるプロファイルの結果， _copy_to_iter が増えるため最適化で省略されていないと考える．
PC 上での動作が安定したため，ラズパイでの実験を行い，結果に応じて動作を高速化させることが必要である．

## 2026/04/17

### 実験

Raspberry Pi 3 を用いて， j2kpay の性能テストを行った．

#### 実験１

 Linux マシンから LAN ケーブルを用いて， RTP パケットを伝送する方法を試したが，
**Raspberry Pi 3 の有線 LAN のポートが 100 Mbps** であるため，伝送に十分なビットレートが実現できなかった．

#### 実験２

Raspberry Pi 3 本体でのローカルループバックと RTPsender を用いて伝送の実験を行った．
しかし， RTPsender の実行速度が想定の半分程度になったため，十分な実験ができなかった．

どちらの実験でも，実行エラーは起こっていないため， 30fps 程度の映像なら解析可能なことがわかった．

### 考察

実験２で RTPsender の性能が十分に出ない原因として，ファイルの i/o を頻繁に行う実装のため，それがボトルネックになった可能性がある．
改善には，プログラム側でバッファを持ち，ファイルの読み出し処理の実行回数を減らすことが挙げられる．

また， kdu_stream_send を Raspberry Pi でビルドして実験に用いるのも検討する．

## 2026/04/18

### プログラム

RelWithDebInfo ビルド時に assert マクロを使ってプログラムの動作をよく確認したところ，***データの排出が間に合っていない可能性が出てきた***．
このときの環境としては， wsl2 + gcc の仮想環境なので可能性の話だが， PC の性能を考慮するとほぼ確実に黒だと考えられる．
簡単な検証として，受信用のバッファをかなり大きく(5000 -> 15000)したが assert マクロでプログラムが停止した．

### 考察

プログラムが停止しないからといって，期待通りの動作をしているわけではないと身をもって学んだ．
改善点として，パースするバッファの切り替え処理や， HTJ2K パケットの解析処理を主として，さらなる高速化が必要である．

今回の検証で得た知見として， デバッグビルド以外でも assert マクロを使う方法とその重要性を得た，
また，非常に大きいメモリを確保する場合には，ローカル変数ではなく，静的変数での確保が有効なことも分かった．
ローカル変数は，プログラム実行中にスタックに積まれるため，非常に大きなメモリを確保しようとするとスタックオーバーフローを起こす．
静的変数では，プログラム起動時にメモリが割り当てられるため，スタックオーバーフローを回避できると考える．

~~信じたくないけどダメそう，冷静に考えるとビルドオプションとポインタ演算少し減らしただけでそこまで高速化するわけない．~~

## 2026/04/19

### プログラム

~~2026/04/14 に解決したと思われた， mutex のロック時間の問題が再浮上した．~~
2026/04/16 での変更によって， J2kBuf::checkFF 関数に生じたバグを排除した．
また，出力したログのチェックを行い，意図した動作をするように修正した．
さらに， RTPsender の RTP パケット送信後に通信終了のパケットを送信するようにし，
j2kpay 側でも通信終了のパケットを受信した場合，プログラムが終了するように改良した．
ログ出力を行わない場合，処理が完了しているのかが実行中に判断できず，意図しない動作を発見できない問題が生じたためこの機能を追加した．

### 考察

ログ出力をしない場合，プログラムの動作が外からでは観測しにくくなるため，適切な処理をあらかじめ作っておくことの重要性を学んだ．

パケットの受信と解析のタイミングをログ出力を用いて分析した結果，大きな同期ズレは観測できなかった．
タイムアウトを指定した場合の， mutex のロックが長すぎるように見える挙動については，ロックを取得したスレッドが片側に偏る場合があったためで，受信処理は問題なく動作していた．
解析処理が遅れていた問題については， 2026/04/16 での変更で J2kBuf::checkFF 関数にバグが入り込んだことが原因だった．

## 2026/04/20

### プログラム

#### 現時点での非同期処理の動作と問題点

#### 受信スレッド

1. socket2.h/recv 関数でテンポラリ用バッファに RTP パケットを受信．
この時点で RTP パケットのシーケンス番号のチェックも行う．  
recv 関数はパケットの到着まで関数を抜けないため，時間がかかる．
その間 mutex を取得していると，解析スレッドが過剰にブロックされてしまうため，ロックの外側で処理．  
2. mutex のロックを取得し，解析スレッドと共有するバッファに，受信したパケットデータをコピー．  
3. 解析スレッドに受信を通知 ( std::condition_variant::notify_all() ) して mutex のロックを解除．

#### 解析スレッド

1. std::condition_variant::wait 関数でパケットの受信を通知されるまで待機．
2. 通知後，未解析のパケットがあれば std::condition_variant::wait 関数を抜ける．このとき mutex のロックを取得する．
3. 未解析のパケットを排出して， mutex のロックを解除．  
排出したパケットを解析処理．

#### 問題点

受信スレッドでの RTP パケットのシーケンス番号の差が 2 ~ 100 程度の範囲でパケットをロストする．

---

recv 関数の動作を**ノンブロッキング**に設定し， mutex の取得後に直接バッファにデータを書き込むように変更した．  
しかし，頻度自体は減ったものの依然として，パケットのロストが発生している．
これは，受信処理のほんの一部を変更しただけに過ぎないため，更に改良が必要だと考える．

### 検証

昨日よりも性能の低い PC(ノートPC1) でコードを実行したところ， RTP パケットのシーケンス番号が 1 ~ 100 程，値が飛んでしまった．
このシーケンス番号のチェックは受信後， mutex のロックをしたときに実行するため，スレッドセーフである．  
また， RTP パケットの送信にインターバル(1us)を挟むと，エラーは発生しなかった．  
RTP は UDP 上で動作すること，昨日の実験で mutex のロックが片側に偏る場合があること，実行性能が低い場合にエラーが起こることを考慮すると，
受信スレッドがロックされているときに次の RTP パケットが到着したため，シーケンス番号のズレが発生したと考えれる．

現在，解析スレッド側がバッファからデータをポップするときに mutex をロックする動作をしているが，

- mutex のロックを取得しないでデータを制御
- mutex のロックが偏らないようにする
- atomic 等，別の非同期処理を使う

などがあげられる．

また．インターバルを挟んで，処理を正常に終了させた場合に，送信したフレーム数によって，各スレッドの終了時刻が変化しており，
バッファのリークには至っていないが，解析スレッドの処理が追い付いていない可能性がある．
ただし，この問題もスレッドの同期処理の改善によって解決する可能性もある．

### 考察

受信処理の改善によって，パケットがロストする問題は解決の兆しが見た．  
ただ，今回の非同期処理で mutex の使用が最善なのかは疑問が残る．
パケットのロストは，受信が行えないタイミングの発生によって引き起こされていると考えている．
そのため，受信スレッドのブロックを最小に抑えることが必要であり，以下のような改善案を列挙する．

- mutex のロックの取得に失敗した場合，待機でなく解析スレッドを一瞬ブロックする等の方法を用いて，受信スレッドを早急に復帰させる．
- atomic 等を用いて，スレッドのブロック自体を最小限に抑える．

## 2026/04/21

### プログラム

mutex のロックにタイムアウトを用いたが，パケットのロストは解決しなかった．  
そのため atomic を使った処理を検討するため，新しいブランチ feature-atomic を作り，プログラムを組むことにした．  
atomic を使った方式でも RTP パケットのロストが起こった．

そこで，アドバイスを受けて RTP パケットの受信処理の最小限の構成で受信機を作り，ログを取ったがパケットのロストが起こった．  
この結果を踏まえて， kakadu のツールの一つの kdu_stream_send を用いて パケットの送信をしたところ，エラーは発生しなかった．

### 考察

パケットロスの原因は 自作の RTPsender だった．送信側は本研究の本筋ではないものの，原因の特定は必要だと考える．

## 2026/04/21

### 検証

Raspberry Pi 4 に mutex と atomic のそれぞれの排他処理を用いたプログラムを実行し，動作確認と性能評価を行った．

- センダーは kakadu の kdu_stream_send
- 送信する映像は 4K (3840 x 2160), 30 bit/pixel, YUV422，60fps

動作検証には，映像データを 120 フレーム (2秒分) 送信し，解析処理を行う．
解析結果のログを記録し，初めの 2 フレームの解析結果を，パケット解析器(動作確認済み)の解析結果と比較する．

性能評価には，映像データを 12000 フレーム (200秒分) 送信し，エラーが発生しないことを確認する．

### mutex

動作検証の結果，受信機は正しく動作した．

性能評価の結果，パケットロスが頻発し，60fps の受信・解析処理を満足に行えなかった．

### atomic

動作検証の結果，受信機は正しく動作した．

性能評価の結果，パケットロスなく，受信・解析処理を実行できた．  
ただ，受信開始時(10フレーム以下)のタイミングは，動作が不安定でパケットロスが発生しやすい．
また，動作が安定した後もまれにパケットロスが発生する．

### 考察

検証結果を踏まえると，排他処理には atomic を用いるのが適していると考えられる．  
しかし，パケットロスの発生が，200秒程度でも起こりうる．さらに，受信開始時の動作が不安定なのは大きな課題であり，排他処理のさらなる改良は必須である．

また，Raspberry Pi 4 の CPU は FPGA の SoC 上に実装される CPU に比べて性能が上回っており，
Raspberry Pi 4 での実行は余裕を持っていることが望ましい．
そのため，受信・解析処理は引き続き高速化が必要だと考える．
万が一，パケットロスが発生した場合でも処理を続行できるようにリカバリー処理の実装も検討している．

動作確認についても，先頭2フレームのみのチェックであるため，可能ならさらにいくつかのフレームで検証を行う．

---

受信開始時の動作が不安定な理由を考察し列挙する．

- スレッドの立ち上げに想定よりも時間がかかっており，各スレッドの処理開始前にパケットが到着している可能性がある．
- 1フレーム目は j2k メインヘッダー の解析処理を実行する必要があり，それの処理時間が影響している．
- 解析スレッドは，パケットの受信まで待機しているが，スレッドの待機と起床の処理に問題がある．

## 2026/04/23

### プログラム

soak test (長時間のテスト) を行い，ログを取るためにもパケットロスのための例外処理を追加する．
そのコードのためのブランチ feature-exception を作った．

#### 実装方針

~~パケットロスが発生した場合，解析スレッド内から例外を投げて解析処理を中断する．~~
~~まず，簡易的な実装として，例外発生時に解析処理を中断して，後続フレームの解析処理に移行する．~~
~~このとき，受信済みの一番新しいメインパケットから再開する．~~
~~これは，例外を補足がボトルネックになり，受信バッファのリークの可能性があるためである．~~

~~パケットのシーケンス番号が前後した場合，受信スレッド内で当該パケットのソートを行う．~~

受信スレッドで RTP パケットロスの確認する．
パケットロスが発生した時点で，ロストしたパケットの構成するフレームを破棄する．

### 検証

#### 9:08
38233 フレームで処理が停止した．センダーの稼働時間は 10m 37s

#### 9:19
6502 フレームで処理が停止した．センダーの稼働時間は 1m 48s

#### 19:12
プログラムを受信スレッドのみの動作に変更し，受信スレッドが 4K 60fps に耐えうるのかを検証
12000 フレーム，3m 20s の受信に成功

#### 18:04
フルHD 24fps の映像で 7440 フレーム， 5m 11s の解析に成功

#### 19:01
上記と同様の条件で 74400 フレーム， 51m 44s の解析に成功

## 2026/04/27

### プログラム
ブランチ test-rtp_analysis を作った． RTP パケットの処理を解析スレッドで行うようにした場合の動作テストと目的としている．

### 検証

- kdu_stream_send で 4K (3840 x 2160), 30 bit/pixel, YUV422，60fps の映像データを送信
- プログラムの実行は Raspberry Pi 4

108000 フレーム 30m 0s の解析に成功

基本的に動作は安定したが以下の課題点が見つかった．

1. プログラム立ち上がり時にパケットロスが起こりやすい．
2. 一度パケットロスが発生すると動作が不安定になることがある．
起動直後に発生したとき， 200 ~ 300 フレームで復帰した．

1の課題点は 04/21 日時点で見つかっている．  
2の課題点は検証不足のため原因がわかっていない．  
考えられる要因として，パケットの破棄を解析スレッドで行っている．
その処理内で atomic 型の変数にアクセス(読み取り1回以上，読み書き1回)しており，これが受信スレッドの動作を妨げている可能性が考えられる．  
解析策として，スレッド間の通信用変数を追加し，パケットの破棄を受信スレッド側で行う．  
一応，そのまま処理を続行した場合，時間をかければ復帰することも確認した．

## 2026/04/28

### 検証

昨日と同様の条件で 648000 フレーム，3h 1s の解析を行った．
プログラムで解析できたフレームは 640484 フレームだった．

# メモ

プロデューサーなんとか  
leaky bucket model  
RFC 9298  
precinct の数え方，再同期ポイント，ハードウェアデコーダに流せる最小単位が RTP パケットからわかるらしい 
-> そこを目安に処理  
受信部分のマルチスレッド化は必須
フレームポインタ

プログラムのアイデア

- 受信スレッドでの RTP パケット memcpy は J2K パケット部分のみで良い可能性
- リングバッファでなく，画像1枚分のバッファを2つ以上用意して，メモリ上に J2K コードストリームを再現する方法
- RTP 関連の処理は受信スレッドで完結
- パケットロスの可能性を考慮すると受信スレッドの負荷は最小限にしたい -> パケットデータをバッファに流すだけ
- パケットロスへの対策は必須であり，エラー耐性の高いコードに改善
- 例外は重いので，関数の戻り値を bool から int へ変更しエラー情報を返す

# アドバイス
recv のブロッキングと**ノンブロッキング**．  
一応ミューテックスはこんなもんだけど，そもそもミューテックスが最適なのか．  
Raspberry Pi の実験では RTPsender は **PC 側**で動かすのが望ましい．
ただし，ラズパイ3b は LANポートの関係で 100Mbps しか出ないので ラズパイ4 or FPGA．
ただし FPGA はすごく低性能 + linux を動かすのが大変らしい．